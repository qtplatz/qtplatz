@echo off
set BOOST_VERSION="${BOOST_VERSION}"

cmake -Wno-dev ^
      -DBOOST_LIBRARYDIR="${BOOST_LIBRARYDIR}" ^
   	 -DBOOST_ROOT="${BOOST_ROOT}" ^
         -DCMAKE_INSTALL_PREFIX="${CMAKE_INSTALL_PREFIX}" ^
      	 -DCMAKE_DEBUG_POSTFIX="d" -G "${GENERATOR}" "${MAEPARSER_SOURCE_DIR}"
