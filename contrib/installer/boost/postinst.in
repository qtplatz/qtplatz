#!/bin/sh

echo "boost post installation..."
echo "installing boost libraries to @TARGET_LIBRARY_DIRECTORY@ ..."

if [ "$1" = "configure" ]; then
    echo "@TARGET_LIBRARY_DIRECTORY@" > /etc/ld.so.conf.d/boost.conf
    ldconfig
fi

